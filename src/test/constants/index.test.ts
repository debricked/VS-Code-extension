// test/constants.test.ts
// import { assert } from 'chai';
// import { COMMANDS, API_ENDPOINTS } from '../../constants';

// suite('Constants > Index : Constants Test Suite', () => {
//     test('COMMANDS should be defined and have expected properties', () => {
//         assert.isDefined(COMMANDS, 'COMMANDS is not defined');
//         assert.property(COMMANDS, 'HELLO_WORLD', 'COMMANDS does not have HELLO_WORLD property');
//         assert.strictEqual(COMMANDS.HELLO_WORLD, 'debricked.helloWorld', 'COMMANDS.HELLO_WORLD does not match expected value');
//     });

//     test('API_ENDPOINTS should be defined and have expected properties', () => {
//         assert.isDefined(API_ENDPOINTS, 'API_ENDPOINTS is not defined');
//         assert.property(API_ENDPOINTS, 'BASE_URL', 'API_ENDPOINTS does not have BASE_URL property');
//         assert.strictEqual(API_ENDPOINTS.BASE_URL, '/api/user', 'API_ENDPOINTS.BASE_URL does not match expected value');
//     });
// });
