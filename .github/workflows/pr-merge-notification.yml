name: Notify on PR Merge

on:
  pull_request:
    types:
      - closed

jobs:
  notify:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest

    steps:
      # - name: Send Slack Notification
      #   env:
      #     SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
      #   run: |
      #     curl -X POST -H 'Content-type: application/json' \
      #     --data '{
      #       "text": "A PR has been merged in the repository *${{ github.repository }}*.\n*PR Title:* ${{ github.event.pull_request.title }}\n*Merged By:* ${{ github.event.pull_request.merged_by.login }}\n*PR URL:* ${{ github.event.pull_request.html_url }}"
      #     }' $SLACK_WEBHOOK_URL

      - name: Send Microsoft Teams Notification
        env:
          TEAMS_WEBHOOK_URL: https://opentextcorporation.webhook.office.com/webhookb2/ed6b2dc3-fef1-47a4-a5c5-fb813e79a713@10a18477-d533-4ecd-a78d-916dbd849d7c/IncomingWebhook/8e304d081dab4769b9d54aaef90a54f6/be055931-5ce9-4c0a-ba25-f49c5b99c18f/V2DE0buolF-U_Vqlz6vaTXe6juCBfs1veoQlLOryiFgbw1
        run: |
          curl -H 'Content-Type: application/json' -d '{
            "@type": "MessageCard",
            "@context": "https://schema.org/extensions",
            "summary": "PR Merged in ${{ github.repository }}",
            "themeColor": "0076D7",
            "title": "Pull Request Merged",
            "sections": [{
              "activityTitle": "A PR has been merged in the repository *${{ github.repository }}*",
              "activitySubtitle": "PR Title: ${{ github.event.pull_request.title }}",
              "activityImage": "https://github.githubassets.com/images/modules/logos_page/GitHub-Mark.png",
              "facts": [{
                  "name": "Merged By",
                  "value": "${{ github.event.pull_request.merged_by.login }}"
                },
                {
                  "name": "PR URL",
                  "value": "[Link](${{ github.event.pull_request.url }})"
                }
              ],
              "markdown": true
            }]
          }' $TEAMS_WEBHOOK_URL
